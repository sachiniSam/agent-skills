{
  "name": "asgardeo-evals",
  "private": true,
  "description": "Promptfoo eval suite for the asgardeo skill (Layer 1: stubbed asg CLI).",
  "scripts": {
    "fixtures": "node scripts/prepare.js",
    "eval": "npm run -s fixtures && PATH=\"$PWD/fixtures/workspace/bin:$PATH\" npx promptfoo@latest eval -c promptfooconfig.yaml -o output.json --no-cache --no-share -j 2",
    "eval:one": "npm run -s fixtures && PATH=\"$PWD/fixtures/workspace/bin:$PATH\" npx promptfoo@latest eval -c promptfooconfig.yaml -o output.json --no-cache --no-share -j 1 --filter-pattern",
    "view": "npx promptfoo@latest view",
    "validate": "npx promptfoo@latest validate config -c promptfooconfig.yaml"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.195"
  }
}
